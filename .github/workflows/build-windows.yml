name: Build Windows EXE

on:
  push:
    branches: ["**"]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller pillow

      - name: Install UPX (for smaller exe)
        run: choco install upx -y --no-progress
        shell: pwsh

      - name: Generate icon.ico from icon.png
        run: python tools/make_icon.py
        continue-on-error: true

      - name: Run tests
        run: |
          pip install pytest
          python -m pytest -q

      - name: Build LizardPDF app folder
        run: pyinstaller build.spec

      - name: Report app folder size
        run: |
          $dir = Get-Item dist/LizardPDF
          $exe = Get-Item dist/LizardPDF/LizardPDF.exe
          $total = (Get-ChildItem $dir -Recurse -File | Measure-Object Length -Sum).Sum
          "LizardPDF folder = {0:N1} MB" -f ($total / 1MB)
          "LizardPDF.exe = {0:N1} MB" -f ($exe.Length / 1MB)
        shell: pwsh

      - name: Report largest bundled files
        run: |
          if (Test-Path dist/LizardPDF) {
            Get-ChildItem dist/LizardPDF -Recurse -File |
              Sort-Object Length -Descending |
              Select-Object -First 30 @{Name="SizeMB";Expression={"{0:N2}" -f ($_.Length / 1MB)}}, FullName |
              Format-Table -AutoSize
          } else {
            "No dist/LizardPDF directory found."
          }
        shell: pwsh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: LizardPDF-windows
          path: dist/LizardPDF
          if-no-files-found: error
